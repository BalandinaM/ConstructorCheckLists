import styled from "styled-components";

export const Wrapper = styled.div`
	flex-grow: 1;
	width: 980px;
	margin: 0 auto;
	padding: 30px 60px;
	/* width: ${props => props.$width || '1120px'};

	padding: 50px 50px;
	text-align: ${props => props.$textalign || 'center'};
	background-color: ${props => props.$bg || null};
	position: relative; */
`;
